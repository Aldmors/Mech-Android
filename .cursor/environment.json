{
  "name": "Mech-Android",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "install": "export ANDROID_HOME=/opt/android-sdk && export PATH=\"$ANDROID_HOME/cmdline-tools/latest/bin:$ANDROID_HOME/platform-tools:$PATH\" && cd android && ./gradlew assembleDebug --no-daemon"
}
